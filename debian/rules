#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	tar -xf debian/prebuilt/ghostty-$(shell dpkg-architecture -qDEB_HOST_ARCH).tar.xz
	mv ghostty debian/
	mv libghostty-dev debian/

override_dh_auto_install:

override_dh_install:
